root = true

[*]
indent_style = space
indent_size = 4
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true

# EF Core migrations are generated code - do not lint them with the same
# strictness as hand-written code (also survives `dotnet ef migrations add`).
[**/Migrations/*.cs]
generated_code = true
